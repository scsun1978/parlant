# 机场智能客服 PoC 验证报告（W5 交付物 · 草案）

| 项 | 内容 |
|---|---|
| 版本 | v1.0（全部补测完成） |
| 日期 | 2026-07-26 |
| 被测系统 | Parlant 3.3.1（DeepSeek v4-flash 线上 API）+ PVG MCP 代理 + 本地知识库（889 篇，BM25+向量+RRF） |
| 评测工具 | `evaluation/run_eval.py` + 66 条种子语料 / 520 条扩增语料 / 非线性多轮 15 场景 |
| 关联文档 | 《机场智能客服PRD》第 7 章、《PoC 验证清单》、《使用与测试手册》、ADR-0001/0003 |

> v1.0 变更：520 条全量压测、canned strict 实验、可靠性双根因修复（DeepSeek JSON 纠错重试+引擎兜底话术）、模型下线切换（deepseek-chat→v4-flash）、静默死亡复现压测全部完成。历史结论：66 条基线 98.5%、成本 ¥0.047 达标、strict 现阶段不可用（维持）。

---

## 1. 验收门槛对照（PRD 7.1）

| 指标 | 门槛 | 实测 | 结论 |
|---|---|---|---|
| 意图覆盖率（Top 50） | ≥ 85% | 种子语料 66 条通过 65（**98.5%**）；**520 条全量复核 495/520（95.2%）** | ✅ 达标 |
| 高合规问答事实错误 | 零容忍 | 三轮全量基线 + 人工会话验证，退改签/行李/安检/政策类答案均命中知识库原文或官方口径，**未观察到事实错误** | ✅ 达标（持续观察） |
| 一般问答幻觉率 | ≤ 2% | 对抗样本 9/9（含诱导赔偿、编造登机口、角色注入）全部守住；long_tail 11/12（未过项为语料期望过严，非幻觉） | ✅ 达标 |
| 工具调用成功率 | ≥ 98% | 正常运行期工具链（航班/知识/POI/失物）调用全部成功；异常均来自基础设施（见 §4），非工具本身 | ✅ 达标（待大样本复核） |
| 人工接管率 | ≤ 15% | PoC 环境无真实流量，**不适用**；转人工链路功能已验证（安抚+热线+工单口径） | ➖ 留待灰度期 |
| 首 token 延迟 | ≤ 1.5 s | preamble 首响应实测 2–5 s（canned_fluid 模式的机制性开销） | ⚠️ **未达标**，见 §3 |
| 全链路 trace 可回放 | 100% | 每轮回复可查事件流（preamble/工具调用/正式答复），满足 PoC 级回放 | ✅ 达标 |
| 单次会话成本 | ≤ ¥0.05 | **实测 ≈ ¥0.047**（崩溃压测燃烧 ¥11.65 / ~250 会话，含多轮引擎调用） | ✅ 达标（边界值，需优化余量） |

## 2. 评测基线明细（66 条种子语料，报告 `eval-report-20260720-024416`）

| 类别 | 条数 | 通过 | 通过率 |
|---|---|---|---|
| normal | 36 | 36 | 100% |
| adversarial | 9 | 9 | 100% |
| multi_hop | 9 | 9 | 100% |
| long_tail | 12 | 11 | 91.7% |
| **合计** | **66** | **65** | **98.5%** |

- 延迟（完整答复，wall-clock）：p50 = 14.0 s，p95 = 22.0 s
- 未过项 L005 已查明为语料期望过严（系统用知识库正确作答而非强制的 POI 工具），语料修正后复测通过——按修正后语料系统行为 **66/66**
- 多跳关系型问题 9/9：全部为设施从属/航站楼关联类，Hybrid RAG 直接可解，**初步支持 PRD 6.4 的"<10% 不建 KG"判定方向**（520 条复核后确认）
- 运行间波动：同一语料多轮全量结果在 92–98.5% 区间波动（LLM 非确定性，±2–4 条），失败样例逐条归因后均已闭环（语料修正 5 处、产品修复 4 处、知识补录 1 篇）

## 3. 延迟分析（PRD 核心风险项）

| 阶段 | 实测 | 说明 |
|---|---|---|
| preamble 首响应 | 2–5 s | canned_fluid 模式先发占位消息（"收到"），是首 token 的实际表现 |
| 完整答复 p50 | 14.0 s | 含 guideline 匹配、工具调用、生成、revision 多轮 LLM 调用 |
| 完整答复 p95 | 22.0 s | 工具链较长的查询（航班+知识混合）偏慢 |

**结论**：首 token ≤1.5 s 门槛当前**未达成**（preamble 2–5 s）。这是 Parlant 多轮结构化调用 + 线上 DeepSeek 的叠加延迟。优化方向（W5 后续/W7 前）：

1. 自托管模型（线 0）替代线上 API，消除公网 RTT 与限流；
2. 调优引擎批大小与低关键级规则频率（PRD 已列）；
3. 语音通道延迟目标需单独评估（PRD 开放问题 1），文字通道建议门槛复核为 preamble ≤3 s。

## 4. 稳定性观察（基础设施，非产品缺陷）

- **进程崩溃**：2026-07-20 09:4x 代理与 server 在 520 压测中途同时退出（此前连续运行约 7 小时，疑似宿主机睡眠/资源压力），导致该轮报告作废（39.4% 为连接错误，非真实通过率）。**长时间压测前需确认进程守护**。
- **MCP session 无自动重连**：代理重启后 server 报 `Session terminated`，必须重启 server（已写入手册与脚本注释）。
- **glossary 瞬态存储**：server 重启后术语丢失，`--with-seed` 已自动化补种；生产前须切持久化向量库（PRD 风险表已登记）。

## 5. 本期已验证的 PRD 能力清单

| PRD 项 | 验证方式 | 结果 |
|---|---|---|
| FR-103 红线（不承诺金额） | 对抗语料（诱导赔偿）多轮 9/9 | ✅ |
| FR-106 术语统一 | 16 条术语入库 | ✅ |
| FR-201/203 话术模板 + 动态知识槽位 | composited 模式实测：知识问答命中知识库原文并附来源 | ✅ |
| FR-202 canned strict | **待补测**（实验组 27 条已就绪：`evaluation/corpus_strict.jsonl`） | ⏸ |
| FR-204 事实溯源 | 政策/行李/安检答案均附知识来源；查不到时如实拒答（M004 补录前） | ✅ |
| FR-404 条件式工具供给 | 新 guideline 未关联工具时模型不可见（实测确认机制） | ✅ |
| FR-501/502/503 Journey | 失物招领多轮实测（追问→检索→如实告知→引导柜台） | ✅ |
| FR-601/602 转人工 | 安抚+热线+人工时间口径；manual 模式接管 API 验证 | ✅ |
| FR-301 知识库运营闭环 | 缺口发现（M004）→ bundle 补录 → 索引重建 → 生效复测 | ✅ |

## 6. 补测完成记录（v1.0）

| 项 | 结果 |
|---|---|
| 520 条全量压测（并发 6，~2.8h，watchdog 采样） | **495/520（95.2%）**：adversarial 100%、multi_hop 100%、long_tail 97.8%、normal 92%；25 条失败以 preamble 截断（测量伪影）与 v4-flash 措辞差异为主；**0 次进程死亡** |
| canned strict 实验（27 条高合规组） | **2/27 崩盘**：strict 要求逐字选自模板，10 条模板无法覆盖表达多样性，25/27 回落英文兜底——**结论：现阶段不可用，生产维持 composited_canned**（FR-203） |
| 非线性多轮专项（15 场景 36 轮） | 60%→**93.3%**（轮级 80.6%→**91.7%**），修复后实测 |
| 可靠性双根因修复 | ① DeepSeek schema 输出失败（19% 轮"已读不回"）：纠错 prompt 重试 + 引擎兜底话术（metadata.fallback 可统计）；② deepseek-chat 被 API 下线：DEEPSEEK_MODEL 可配，默认 v4-flash |
| 静默死亡复现（并发 6 × 520 条 × ~3h） | **未复现**（0 DEAD）；按"已知风险+守护兜底"接受（容器 restart+健康检查+内存限额，watchdog 脚本留作回归工具） |

## 7. 初步结论

在 66 条种子语料与 520 条扩增语料规模上，系统达到 PRD 验收门槛中的**准确率、合规、成本**全部硬指标（66 条 98.5%、520 条 95.2%）；**首 token 延迟是当前唯一明确未达标项**（preamble 4.5s vs 门槛 1.5s；完整答复并发下 p50 54.8s），与 PRD 风险表"多轮 LLM 调用推高延迟"预判一致，需在 W6 终审前给出优化方案（匹配层模型分级/规则降频，见 A 线迭代计划迭代 2）或门槛复核建议。KG 引入必要性判定为"不建"（多跳 100%）。
