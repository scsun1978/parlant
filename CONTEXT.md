# 机场智能客服（PVG）

浦东机场小程序智能客服与数字人客服的建设语境。当前存在两条同源的技术路线（A=Parlant harness 线、B=自研 hermes runtime 线），共享同一份知识发布（pvg-v3-knowledge-release）。

## Language

### 会话与规则

**Guideline（规则）**:
以"当 X 则 Y"自然语言声明的客服 SOP，引擎每轮全量重匹配。Parlant 线的核心治理单元。
_Avoid_: 流程脚本、if-else 规则

**话术模板（Canned Response）**:
经预审批的回复模板，固定口径或含动态槽位（`{{knowledge_answer}}` 等）。高合规场景必须使用。
_Avoid_: 自由生成、即兴回答

**组合模式（Composition Mode）**:
回复生成策略：`fluid`（自由生成）、`canned_fluid`（有模板用模板）、`composited_canned`（模板骨架+动态数据渲染）、`strict_canned`（只能逐字选模板）。PoC 结论：strict 现阶段不可用，高合规用 composited。
_Avoid_: 严格模式（歧义，须指明枚举值）

**Journey（多步流程）**:
可跳步/反悔/恢复的多步引导（失物招领、投诉转人工、退改签）。不是状态机，旅客意图随时优先。
_Avoid_: BPMN 流程、工单流

**红线（Red Line）**:
"绝不允许"类规则（不得承诺赔偿金额、不得评价竞争对手、检索为空不得生成）。高关键级，违反即转人工或兜底。
_Avoid_: 兜底、降级（那是另一回事）

**条件式工具供给**:
工具仅在其关联 guideline 命中时对模型可见，未关联不得调用。
_Avoid_: 全量工具列表

### 质量与评测

**高合规问答**:
退改签/行李/安检/延误政策类问题，事实错误零容忍，答案必须命中知识库原文或官方口径。
_Avoid_: 普通 FAQ

**preamble（占位消息）**:
引擎在正式答复前发出的即时确认（"收到/Got it"）。不是答复，评测与人工判读都不得把它当答案。
_Avoid_: 首条回复（歧义）

**静默窗口**:
判定一轮答复完成的时间窗（连续 15 秒无新事件）。preamble 与多段答复的存在使"取首条消息"成为错误口径。

**PASS / PARTIAL / GAP**:
B 线评测判定：PASS=全部期望满足；PARTIAL=主要期望满足但有缺项；GAP=意图/事实性失败。A 线口径为 通过/未通过（must_include 全命中且红线未命中）。
_Avoid_: 混用两口径直接比较（98.5% vs 74% 不是能力差距）

**交叉验证（bake-off）**:
同一语料打两个系统、同一评分口径对照的方法。比较"实现效果"的唯一诚实途径。

### 知识与检索

**知识发布（Knowledge Release）**:
经评审的知识资产不可变快照（887/888 篇，含 sha256 与有效期），两条路线共用同一发布物。实时/交易数据（航班、余票）不进知识库。
_Avoid_: 知识库（那是存储形态，不是发布行为）

**Hybrid RAG**:
BM25（中文分词）+ 向量 + RRF 融合的混合检索；召回测试台要求 BM25/向量/RRF 分阶段可视。

### 角色与渠道

**旅客（C 端）**:
小程序或数字人终端的使用者。
_Avoid_: 用户、客户（B 端角色时歧义）

**转人工（人工接管）**:
旅客要求、投诉意图、连续未解决或命中红线时的接管；接管期间 AI 静默，坐席可见完整上下文。session mode=manual 为 A 线实现，handoff_to_human 工作流为 B 线实现。

**数字人通道**:
外部合作方承载 ASR/TTS/形象的语音通道，客服系统只出 speech_text 与呈现指令（动作码）。双方当前均无自研语音管线。
_Avoid_: 语音客服（暗示自建 ASR/TTS）
